# Minimal safe test fixture for deez-dots
[global]
version = "0.0.1"
owner = "Test"
description = "Minimal test fixture config used for config-only CLI flows."
home = "/tmp/test/home"
# No git source needed for config-only operations
# This config is used by tests that exercise deps and config loading only.

dots = ["kitty"]

[kitty]
description = "Small kitty sample used by tests and quick manual checks."
source_root = "Configs/.config"
target_root = "/tmp/test/home/.config"
paths = "kitty"
